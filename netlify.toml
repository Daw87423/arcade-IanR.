[build]
  publish = "public"
  functions = "netlify/functions"

# Le frontend appelle /api/login, /api/signup, /api/score, /api/admin
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
